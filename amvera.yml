# Конфигурация для Amvera Cloud
meta:
  environment: node
  toolchain:
    name: npm
    version: 22
build:
  additionalCommands: "npm run build"
run:
  command: npm run start
  persistence:
    - path: /data
      shared: true
