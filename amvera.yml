# Конфигурация для Amvera Cloud
meta:
  environment: node
  toolchain:
    name: npm
    version: 20
build:
  additionalCommands:
    - npm run build
run:
  command: npm run start
  persistence:
    - path: /data
      shared: true
