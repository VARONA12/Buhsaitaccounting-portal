# Build ID: 1773882747
meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  additionalCommands: "npm install && rm -rf .next && npm run build && cp -r public .next/standalone/ && mkdir -p .next/standalone/.next && cp -r .next/static .next/standalone/.next/"

run:
  command: "HOSTNAME=0.0.0.0 PORT=3000 NODE_OPTIONS='--max-old-space-size=320' node .next/standalone/server.js"
  containerPort: 3000
