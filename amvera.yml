# Конфигурация для Amvera Cloud
meta:
  environment: node
  toolchain:
    name: npm
    version: 22
build:
  additionalCommands:
    - npm run build
run:
  command: npx next start -p 3000 -H 0.0.0.0
  containerPort: 3000
