# Конфигурация для Amvera Cloud
meta:
  environment: node
  toolchain:
    name: npm
    version: 22
build:
  additionalCommands: "npm run build"
run:
  command: "npx prisma generate && node .next/standalone/server.js"
  containerPort: 3000
